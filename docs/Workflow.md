# Workflow

- Branching: `feature/*`, `fix/*`, `chore/*`; PRs require review.
- Commits: Conventional messages; reference docs and tests.
- Releases: Tag versions; changelog updates; publish images (future).
- Environments: Local dev (compose), staging (soon), prod (planned).
